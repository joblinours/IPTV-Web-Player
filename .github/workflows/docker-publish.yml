name: Docker Publish

on:
  push:
    branches:
      - main
      - master
      - dev
      - feature/movix-improvements
    tags:
      - "v*"
    paths:
      - "backend/**"
      - "frontend/**"
      - ".github/workflows/docker-publish.yml"
  workflow_dispatch:

env:
  REGISTRY: docker.io
  IMAGE_NAMESPACE: ${{ secrets.DOCKERHUB_USERNAME }}

jobs:
  build-and-push:
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false
      matrix:
        include:
          - service: backend
            context: ./backend
            dockerfile: ./backend/Dockerfile
            image: iptv-web-player-backend
            build_args: ""
          - service: frontend
            context: ./frontend
            dockerfile: ./frontend/Dockerfile
            image: iptv-web-player-frontend
            build_args: |
              VITE_API_BASE_URL=${{ vars.VITE_API_BASE_URL }}

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up QEMU
        uses: docker/setup-qemu-action@v3

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Log in to Docker Hub
        uses: docker/login-action@v3
        with:
          username: ${{ secrets.DOCKERHUB_USERNAME }}
          password: ${{ secrets.DOCKERHUB_TOKEN }}

      - name: Extract Docker metadata
        id: meta
        uses: docker/metadata-action@v5
        with:
          images: ${{ env.REGISTRY }}/${{ env.IMAGE_NAMESPACE }}/${{ matrix.image }}
          tags: |
            type=raw,value=latest,enable={{is_default_branch}}
            type=raw,value=dev,enable=${{ github.ref == 'refs/heads/dev' || github.ref == 'refs/heads/feature/movix-improvements' }}
            type=ref,event=branch
            type=sha
            type=ref,event=tag

      - name: Compute build metadata
        id: build_meta
        run: |
          echo "commit=${GITHUB_SHA::7}" >> "$GITHUB_OUTPUT"
          echo "version=$(jq -r .version ${{ matrix.context }}/package.json)" >> "$GITHUB_OUTPUT"

      - name: Build and push image
        uses: docker/build-push-action@v6
        with:
          context: ${{ matrix.context }}
          file: ${{ matrix.dockerfile }}
          push: true
          build-args: |
            ${{ matrix.build_args }}
            APP_VERSION=${{ matrix.service == 'backend' && steps.build_meta.outputs.version || '' }}
            GIT_COMMIT=${{ matrix.service == 'backend' && steps.build_meta.outputs.commit || '' }}
            VITE_APP_VERSION=${{ matrix.service == 'frontend' && steps.build_meta.outputs.version || '' }}
            VITE_GIT_COMMIT=${{ matrix.service == 'frontend' && steps.build_meta.outputs.commit || '' }}
          tags: ${{ steps.meta.outputs.tags }}
          labels: ${{ steps.meta.outputs.labels }}
          cache-from: type=gha
          cache-to: type=gha,mode=max
